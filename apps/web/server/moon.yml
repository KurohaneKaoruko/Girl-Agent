$schema: "https://moonrepo.dev/schemas/project.json"
language: "rust"

tasks:
  check:
    command: "cargo"
    args:
      - "check"
      - "-p"
      - "girl-ai-agent-web-server"

  dev:
    command: "cargo"
    args:
      - "run"
      - "-p"
      - "girl-ai-agent-web-server"

