{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for GirlAgent app host.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
