$schema: "https://moonrepo.dev/schemas/project.json"
language: "rust"

tasks:
  check:
    command: "cargo"
    args:
      - "check"
      - "-p"
      - "girlagent-app"

  dev:
    command: "cargo"
    args: ["tauri", "dev"]
